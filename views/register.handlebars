<div class="registration">
    {{#if error}}
    <h2 class="loginerror">Something went wrong!! Please check if you have filled all the required areas!!!!</h2>
    {{/if}}
    <div class="logincontainer">
        <div class="smallintro">
            <p>Sign this petition to legally laugh like a Denver (Money heist)! </p>
            <p>Please watch the video before you register. #laughandlive</p>
            <div class="vid">
                <iframe width="500" height="300" src="https://www.youtube.com/embed/MASPbl210_c" frameborder="0"
                    allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen>Denver's
                    laugh</iframe>
            </div>

        </div>
        <p id="regipara">Please insert your details here!</p>
        <form method="POST">
            <input type="text" class="userinput" name="firstname" placeholder="First Name">
            <input type="text" class="userinput" name="lastname" placeholder="Last Name">
            <input type="text" class="email" name="email" placeholder="email">
            <input type="password" class="password" name="password" placeholder="password">
            <input type="hidden" name='_csrf' value="{{csrfToken}}">
            <button>Submit</button>
            <a class="loginpage" href="./login">If you have already register. Login Here!</a>
        </form>

    </div>


</div>